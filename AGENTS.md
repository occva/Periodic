# Subscription 项目 Agent 指南

本规范适用于仓库根目录及其全部子目录。目标是持续交付易懂、易测试、易扩展的原生 macOS 应用；实现功能时必须同时维护清晰的职责边界，禁止以快速堆积代码代替设计。

## 1. 信息来源与目录职责

开始任务前，根据影响范围阅读对应目录，不依赖某一个固定文件名：

- `docs/`：产品需求、业务规则、交互设计、数据口径和验收标准。产品行为以这里为准。
- 应用源码目录：当前实现和架构事实。文档描述目标，不代表功能已经落地。
- `Tests/`：已有测试约束、测试工具和回归边界。
- `Configurations/`：平台版本、编译、签名、沙盒及并发配置。
- `script/`：项目提供的构建、运行和测试入口。
- 工程配置目录：target、scheme、资源归属和构建设置。

文档与代码冲突时，先区分“目标设计尚未实现”和“实现偏离需求”。需求冲突或语义不明确时说明问题，不自行创造产品规则。目录结构调整后，以新目录职责为准，同步更新本规范中已经失效的描述。

## 2. 平台与技术边界

- 使用 Swift 6、SwiftUI、SwiftData、Swift Charts 和 UserNotifications，最低支持 macOS 26。
- 保持 Swift 严格并发检查；新增代码必须有明确的 actor 隔离和跨并发域数据边界。
- 产品是离线个人工具，不引入账号、云同步、联网汇率、HTTP 服务、遥测或自动扣款。
- 默认不增加第三方依赖、在线资源或新的系统权限。确有必要时，先说明用途、替代方案、维护成本和隐私影响。
- 使用系统原生组件、语义化样式和 macOS 标准交互，不自行复刻已有系统能力。
- 文件访问遵循 App Sandbox，通过用户授权的系统入口完成，不扩大权限绕过限制。
- 工程名、target、scheme、bundle identifier、签名和数据存储位置属于全局配置；非任务明确要求不得顺手修改。

## 3. 架构与依赖方向

代码按职责分层，依赖保持单向：

```text
App 组装与路由
       ↓
View / Feature 状态
       ↓
Service / Use Case
       ↓
Domain 规则与值类型
       ↓
Persistence / System Adapter
```

- 上层可以依赖下层提供的协议和模型；下层不得反向依赖具体页面、弹窗或视图状态。
- App 层只负责生命周期、依赖装配、场景和顶层路由，不承载业务计算。
- View 负责渲染状态和发送用户意图，不直接编写数据库事务、复杂校验、金额计算或日期算法。
- Feature/Store 管理单个功能的页面状态、异步任务和用户意图协调；不得演变为包含全应用逻辑的巨型 Store。
- Service/Use Case 表达完整业务操作和事务边界，不暴露持久化实现细节给 UI。
- Domain 层使用纯值类型和纯函数承载业务规则，尽量不依赖 SwiftUI、SwiftData 或系统单例。
- Persistence 和系统适配层负责数据库、文件、通知等副作用，通过窄接口向上提供能力。
- 跨模块复用依赖稳定的业务抽象，不允许模块之间直接读取对方内部状态或复制一套近似逻辑。
- 共享依赖在应用入口统一装配；禁止随处创建数据库容器、服务实例或隐式全局单例。

## 4. 拆分、解耦与扩展原则

- 一个类型只承担一种主要职责。出现多个互不相关的变化原因时，应拆分而不是继续追加分支。
- 新功能先确认属于现有模块还是独立能力，再决定扩展接口或新增类型；不要默认往最大文件、根视图或公共工具类中追加。
- 以业务能力组织代码，不以“所有模型”“所有工具”“所有弹窗”形成无边界的大杂烩。
- 公共抽象至少应解决两个真实且稳定的重复场景。不要为猜测中的未来需求提前设计复杂框架。
- 避免万能 Manager、Helper、Utils、Coordinator。名称必须体现负责的业务概念和动作范围。
- 依赖具体实现会妨碍测试或替换时，使用小而聚焦的协议；不要给每个类型机械创建协议。
- 优先组合而非继承，优先显式数据流而非通知广播和隐式共享状态。
- 功能开关、状态枚举和路由必须可穷举；不要用松散字符串驱动核心业务。
- 单个函数保持一个抽象层级。校验、转换、持久化和 UI 展示混在一起时，应提取清晰步骤。
- 条件分支持续增长时，先寻找缺失的领域类型、策略或状态机，不用更多布尔参数维持复杂度。
- 变更公共接口前检查全部调用方和测试；新增能力不应迫使无关模块一起修改。
- 删除已经不用的兼容层、重复实现和死代码，但不得借重构扩大任务范围或覆盖用户改动。

## 5. 统一 Swift 编码规范

### 5.1 命名

- 类型、协议和枚举使用 `UpperCamelCase`；变量、函数、属性、参数和常量使用 `lowerCamelCase`。
- 文件通常按主要类型或功能命名；名称与职责一致，一个主要类型优先放在一个文件中。
- 使用完整、准确的英文标识符，禁止拼音、无意义缩写、类型重复和模糊词。
- 布尔值以 `is`、`has`、`can`、`should` 等表达肯定语义，避免双重否定。
- 函数使用动词或动词短语，类型使用名词；协议描述能力或角色，不统一添加无意义后缀。
- 缩写遵循 Swift API Design Guidelines，在同一代码库内保持一致。

### 5.2 格式与结构

- 使用 4 个空格缩进，不混用 Tab；花括号、空行和换行风格保持统一。
- 长表达式按参数或语义分组换行，不为了机械行宽破坏可读性。
- 文件内部按公开接口、初始化、状态、行为、私有辅助的稳定顺序组织；相近逻辑放在一起。
- 访问控制取满足需求的最小范围，默认从 `private` 开始，不无理由暴露可变状态。
- 优先 `let`、值类型和不可变输入输出；修改状态必须集中且可追踪。
- 禁止强制解包、强制转换和 `try!`，除非由编译期或封闭条件严格保证，并写明原因。
- 禁止魔法数字和散落的用户文案。业务常量应有领域含义，但不要把仅使用一次且语义清楚的局部值过度抽象。
- 不保留注释掉的代码、无效占位、重复实现、调试输出或无用途的 import。

### 5.3 函数与类型复杂度

- 函数应短小并聚焦一个结果；参数过多时使用有含义的请求值类型，不使用难懂的位置参数或布尔参数串。
- 类型过大、属性过多或同时处理 UI、业务、存储时必须拆分。拆分按职责和数据所有权进行，不按固定行数机械切文件。
- `switch` 应尽量穷举；未知状态要显式处理，不以 `default` 隐藏新增枚举带来的行为缺失。
- 重复业务判断提取到唯一规则入口；仅语法相似但语义不同的代码不要强行共用。
- 优先早返回降低嵌套；错误路径与成功路径清楚分离。
- 公共 API 使用领域类型表达约束，避免到处传裸字符串、裸字典和彼此易混淆的基础类型。

### 5.4 注释与错误处理

- 注释解释业务原因、边界条件和非直观取舍，不逐行翻译代码，也不强制作者署名。
- 对外契约和复杂算法使用文档注释；临时兼容必须说明原因和移除条件。
- 错误使用具体、可恢复的类型向上传递，保留底层原因并映射为用户可理解的提示。
- 禁止静默吞错、用空数据伪装读取失败、无提示回退或通过清库恢复。
- 只在能够补充上下文、重试、降级或转成领域错误的层级捕获错误，不做无意义的捕获再抛出。

## 6. 状态、并发与持久化

- App 级事实、窗口会话状态、页面临时状态和持久偏好必须分开管理，避免状态提升过度。
- UI 状态更新在 `MainActor`；耗时聚合和 I/O 不阻塞主线程。
- 跨 actor 传递的值满足 `Sendable`。不得用 `@unchecked Sendable` 掩盖设计问题，除非同步机制可证明并有说明。
- 可取消的搜索、统计和范围切换使用任务取消及 generation/version 校验，旧响应不得覆盖新状态。
- 数据库写入通过唯一存储入口串行化；一项用户操作涉及的多次写入应有明确事务边界。
- 多窗口写入使用 revision、数据集版本或等价机制识别冲突，不允许旧快照静默覆盖新数据。
- 数据模型使用显式版本和迁移方案；迁移或打开失败必须保留原数据，不得删除正式数据库或静默改用内存库。
- 数据库与图片等外部文件共同变更时，明确提交顺序、失败补偿和可重试策略。
- 表单使用独立草稿，取消不产生写入；保存期间防止重复提交，失败后保留用户输入。

## 7. 核心业务不变量

- 管理状态与按日期派生的到期状态是不同概念；停用不等于过期。
- 周期订阅与终生购买使用明确类型表达。终生项目没有到期日和周期月数，不续费、不提醒、不参与周期费用预估。
- 当前价格、周期报价和实际付款是不同事实，不能互相覆盖，也不能从其中一项伪造另一项历史。
- 金额使用最小货币单位或 `Decimal` 精确计算，禁止用 `Double` 保存、比较或汇总金额；图形坐标转换除外。
- 不跨币种合计，不隐式换汇；输入、存储、计算和展示均尊重币种精度。
- 业务日期使用本地日历日语义，明确时区和首尾包含关系；禁止用固定秒数近似日、月或年。
- 到期日当天仍有效且剩余 0 天，次日才过期；续费、时间线和统计必须复用同一日期规则。
- CSV、恢复、续费、删除等复合写入先预览再确认，并保证原子性或明确的补偿行为。
- 普通编辑不自动生成付款或周期历史；历史记录的纠正不应隐式改写当前设置。
- 业务细节和完整边界以 `docs/` 中对应模块为准，不把本节摘要当成全部需求。

## 8. UI、可访问性、日志与隐私

- 优先使用原生导航、表格、设置、弹窗、工具栏和语义化颜色，保持键盘、菜单及多窗口行为符合 macOS 习惯。
- 窄窗口使用合理重排、滚动和系统溢出，不通过隐藏必要字段规避布局问题。
- 颜色不能是唯一状态提示；交互元素提供可读标签、键盘操作、焦点顺序和 VoiceOver 语义。
- 支持浅色/深色外观、Reduce Motion、可缩放内容和长文本，不以固定像素假设替代自适应布局。
- 使用 OSLog 体系记录必要的生命周期、性能和故障上下文，不用裸 `print` 作为生产日志。
- 日志不得包含完整备注、导入内容、金额明细、文件内容或其他敏感数据，也不得上传网络。
- 用户可见错误要说明结果、影响和恢复动作；内部技术细节只进入受控日志。

## 9. 测试与验证

- 每次改动按影响范围使用 `script/` 中现有入口完成最小充分的构建和测试，不在规范中绑定具体脚本文件名。
- 业务规则、值类型、日期、金额、筛选、迁移和服务事务优先使用单元测试。
- 导航、窗口、表格选择、弹窗、键盘操作及可访问性关键路径使用 UI 测试。
- 金额测试覆盖币种精度、零值、舍入和溢出；日期测试覆盖今天、月末、闰年、跨年和时区变化。
- 数据测试覆盖事务回滚、重复提交、版本冲突、恢复失败和数据集切换。
- 修复缺陷时优先添加能复现问题的回归测试，再验证修复；不得降低断言或修改测试来掩盖回归。
- 环境无法执行某类验证时，明确记录未运行项和原因，不将其描述为通过。

## 10. 变更纪律

- 修改前检查工作区，保留用户已有及无关改动；只编辑当前任务需要的范围。
- 不执行破坏性 Git 操作，不删除正式用户数据，不提交构建产物、测试结果或本机缓存。
- 新功能必须放入正确职责层，并同步必要的测试和 `docs/`；禁止把“先跑起来”作为长期耦合的理由。
- 重构以可观察行为不变为前提，分步进行并保持每一步可验证；不要把大规模重构夹带进小功能。
- 公共接口、数据模型或目录职责改变时，同步调用方、迁移、测试和文档。
- 提交信息使用统一的语义前缀，如 `feat:`、`fix:`、`refactor:`、`docs:`、`test:`、`chore:`，描述单一、清晰的变更目的。
